<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.group395.ember">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/emberlg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/emberlg_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SuggestionsActivity"></activity>
        <activity android:name=".SearchStartActivity" />
        <activity android:name=".SearchResultsActivity" />
        <activity android:name=".MoviePageActivity" />
        <activity android:name=".SearchOptionsActivity" />
        <activity android:name=".HistoryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>